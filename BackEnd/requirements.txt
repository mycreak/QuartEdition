# QuartEdition — 后端 Python 依赖
# Python >= 3.10
# 安装: pip install -r requirements.txt

# ── Web 框架 ──
quart>=0.19.0
quart-schema[pydantic]>=0.22.0
quart-cors>=0.7.0
hypercorn>=0.11.2

# ── 数据库 ──
aiomysql>=0.2.0
motor>=3.3.0
redis>=4.2.0

# ── 认证 ──
bcrypt>=4.0.0
PyJWT>=2.6.0

# ── 数据校验与配置 ──
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── 爬虫 ──
playwright>=1.40.0
aiohttp>=3.9.0
yarl>=1.9.0
brotli>=1.1.0

# ── 对象存储 ──
tos>=2.9.0

# ── 系统监控 ──
psutil>=5.8.0

# ── 测试 ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
