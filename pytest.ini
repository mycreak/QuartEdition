[pytest]
asyncio_mode = auto
markers =
    e2e: end-to-end tests that make real network requests (requires Playwright + databases)
    auth_integration: auth integration tests (requires MySQL)
testpaths = BackEnd/test