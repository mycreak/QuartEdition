# QuartEdition — 项目依赖入口
#
# 本项目分前后端，各自有独立的依赖文件：
#
#   后端 (Python)
#     pip install -r BackEnd/requirements.txt
#
#   前端 (Node.js)
#     cd FrontEnd && npm install
#     可读清单: FrontEnd/requirements.txt
#
